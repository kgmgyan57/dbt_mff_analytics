
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'duckdb_analytics'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'duckdb_analytics'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# In this example config, we tell dbt to build all models in the example/
# directory as views. These settings can be overridden in the individual model
# files using the `{{ config(...) }}` macro.

models:
  duckdb_analytics:
  # Everything in the /bronze folder goes to the 'raw' schema
    bronze:
      +schema: raw
      +materialized: view
    
    # Everything in the /silver folder goes to the 'silver' schema
    silver:
      +schema: silver
      +materialized: table
    
    # Everything in the /gold folder goes to the 'gold' schema
    gold:
      +schema: gold
      +materialized: table

# For production, we can split to 3 different databases for better data management and security. We can also enable or disable the elementary models based on an environment variable.
# models:
#   duckdb_analytics:
#     bronze:
#       +enabled: true
#       +database: "maven-bronze"
#     silver:
#       +enabled: true
#       +database: "maven-silver"
#     gold:
#       +enabled: true
#       +database: "maven-gold"

#   elementary:
#     +enabled: "{{ env_var('ENABLE_ELEMENTARY', 'true') == 'true' | as_bool }}"
#     +database: "maven-silver"
#     +schema: maven_observability